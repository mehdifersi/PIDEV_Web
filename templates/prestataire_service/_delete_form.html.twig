<form method="post" action="{{ path('app_prestataire_service_delete', {'id': prestataire_service.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prestataire_service.id) }}">
    <button class="btn">Delete</button>
</form>
