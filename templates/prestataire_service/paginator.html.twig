{%  extends 'templatefront.html.twig' %}
{% block body %}

    {% for article in donnees %}

        {% include 'prestataire_service/article.html.twig'  %}
        {% include '@KnpPaginator/Pagination/bootstrap_v5_bi_sortable_link.html.twig' %}
    {% endfor %}

    <div class="container">
        <div class="row">
            <div class="d-flex align-items-center justify-content-center">
                {{ knp_pagination_render(donnees) }}
            </div>
        </div>
    </div>
{% endblock %}